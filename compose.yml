include:
  - services/3xui/compose.yml

services:
  # PROXY
  traefik:
    image: traefik:v3.4
    restart: always
    container_name: traefik
    user: "0:0"  # Run as root to access the acme.json file
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./traefik.toml:/etc/traefik/traefik.toml
      - ./acme/acme.json:/acme/acme.json
  redirect-service:
    image: traefik/whoami  # Lightweight image, won't actually be used
    labels:
      - traefik.enable=true
      # Redirect base URL to external site
      - traefik.http.routers.redirect-root.rule=Host(`${HOST}`) && Path(`/`)
      - traefik.http.routers.redirect-root.entrypoints=web
      - traefik.http.routers.redirect-root.middlewares=redirect-to-external
      - traefik.http.middlewares.redirect-to-external.redirectregex.regex=^.*
      - traefik.http.middlewares.redirect-to-external.redirectregex.replacement=https://contract.gosuslugi.ru/
      - traefik.http.middlewares.redirect-to-external.redirectregex.permanent=true

volumes:
  acme:
